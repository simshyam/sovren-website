# Conventions

- Use descriptive branch names (e.g. `fix-header-nav`, `add-portfolio-page`), not generic ones like `patch` or `update`.
- Write clear, descriptive commit messages explaining why a change was made, not just what changed.
- Never push directly to `main`. Always create a branch and open a PR.
- Never modify files outside this repo.
